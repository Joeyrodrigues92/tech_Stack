// This of 1 of2 reducers we create for this app

// *LibraryReducer*

// Import our data

// Will hold an the data in an array of objs. Each obj represents on single library. 

// Each obj will have and id, name, and description key for each obj

import data from './LibraryList.json';

export default () => data;

